version: '3.8'

services:
  licita-agil-landing:
    build: .
    container_name: licita-agil-landing
    restart: always
    expose:
      - "80"
    networks:
      - proxy_network

networks:
  proxy_network:
    external: true
